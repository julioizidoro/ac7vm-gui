import { Clientecomplemento } from './clientecomplemento';

describe('Clientecomplemento', () => {
  it('should create an instance', () => {
    expect(new Clientecomplemento()).toBeTruthy();
  });
});
